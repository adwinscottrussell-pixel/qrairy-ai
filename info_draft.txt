      info: (function(){
        if (!si||(!si.address&&!si.phone&&!si.website&&!si.hours&&!si.email)) return '';
        var rows=[], rs='display:flex;align-items:flex-start;gap:10px;padding:8px 0;border-bottom:0.5px solid '+cardBorder+';', rl='display:flex;align-items:flex-start;gap:10px;padding:8px 0;', ts='font-size:.75rem;color:'+textColor+';line-height:1.4;';
        if(si.address&&si.addressEnabled!==false) rows.push('<div style="'+rs+'"><span>📍</span><span style="'+ts+'">'+si.address+'</span></div>');
        if(si.phone&&si.phoneEnabled!==false)     rows.push('<div style="'+rs+'"><span>📞</span><span style="'+ts+'">'+si.phone+'</span></div>');
        if(si.website&&si.websiteEnabled!==false)  rows.push('<div style="'+rs+'"><span>🌐</span><span style="'+ts+'">'+si.website+'</span></div>');
        if(si.hours&&si.hoursEnabled!==false)      rows.push('<div style="'+rs+'"><span>🕒</span><span style="'+ts+'">'+si.hours.replace(/\n/g,'<br>')+'</span></div>');
        if(si.email&&si.emailEnabled!==false)      rows.push('<div style="'+(rows.length?rl:rs)+'"><span>✉</span><span style="'+ts+'">'+si.email+'</span></div>');
        if(!rows.length) return '';
        return '<div data-section="info" style="padding:8px 20px 16px;"><div style="background:'+cardBg+';border:0.5px solid '+cardBorder+';border-radius:16px;padding:16px;">'+rows.join('')+'</div></div>';
      })(),

